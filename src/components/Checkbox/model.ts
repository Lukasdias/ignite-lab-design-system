import { ReactNode } from 'react'
import { Slot, SlotProps } from '@radix-ui/react-slot'

export interface CheckboxRefModel {
    checked: boolean
    handleCheck: () => void
}
export interface CheckboxModel {}
